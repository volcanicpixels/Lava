default:
  class: default

primary:
  class: primary

submit:
  type: submit
  name: action
  add_form: true


update:
  extends: primary, submit
  value: save
  text: Update Settings

preview:
  extends: default, submit
  text: Preview Settings