{% extends 'Default.twig' %}

{% block setting_value %}
	<textarea class="cinder-auto-resize actual-input" name="{{ setting_name }}" {% for attr, the_value in setting_input_attrs %} {{ attr }}="{{ the_value }}"{% endfor %}>
		{{- setting_value -}}
	</textarea>
{% endblock %}