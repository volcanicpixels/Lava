{% block button %}
	<button type="{{ type }}" name="{{ name }}" class="{% block class %}cinder-btn{% endblock%}" value="{{ value }}" {% for attr, value in attrs %} {{ attr }}="{{ value | e }}"{% endfor %}>
		{{ text | default( 'undefined' ) }}
	</button>
{% endblock %}