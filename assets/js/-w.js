// Generated by CoffeeScript 1.3.3
(function() {
  var $, defaults, document, hashMethods, methods, window;

  $ = jQuery;

  window = window;

  document = document;

  methods = {};

  hashMethods = {};

  defaults = {};

  $.fn.extend({
    lava: function(method) {
      var log, settings;
      settings = {
        option1: true,
        option2: false,
        debug: false
      };
      settings = $.extend(settings, options);
      log = function(msg) {
        if (settings.debug) {
          return typeof console !== "undefined" && console !== null ? console.log(msg) : void 0;
        }
      };
      return this.each(function() {
        log("Preparing magic show.");
        return log("Option 1 value: " + settings.option1);
      });
    }
  });

}).call(this);
